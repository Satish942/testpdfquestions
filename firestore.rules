rules_version = '2';
// Exam history is written only via Firebase Admin SDK (server). Block direct client access.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
